
Процедура ПередВыполнением(Отказ)
	Исполнитель = ПараметрыСеанса.ТекущийПользователь;
КонецПроцедуры

Процедура ПриВыполнении(Отказ)
	Если ПолученоСогласие Тогда
		ЗаблокироватьДанныеДляРедактирования(БизнесПроцесс);
		Объект = БизнесПроцесс.ПолучитьОбъект();
		Объект.РегистрацияПодтверждена = Истина;
		Объект.Записать();
		РазблокироватьДанныеДляРедактирования(БизнесПроцесс);
	КонецЕсли;
КонецПроцедуры
